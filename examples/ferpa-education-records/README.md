# FERPA education records example

Validates the `ferpa-school-official.yaml` template structure for EdTech agent deployments.

```bash
python examples/ferpa-education-records/validate_ferpa_scope.py
```

Skill: `ferpa-education-records`
